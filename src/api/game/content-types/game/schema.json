{
  "kind": "collectionType",
  "collectionName": "games",
  "info": {
    "singularName": "game",
    "pluralName": "games",
    "displayName": "Wordle Puzzle",
    "description": "Daily Wordle words with dates"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "word": {
      "type": "string",
      "required": true,
      "minLength": 3,
      "maxLength": 8
    },
    "date": {
      "type": "date",
      "unique": true,
      "required": true
    },
    "hint": {
      "type": "string"
    }
  }
}