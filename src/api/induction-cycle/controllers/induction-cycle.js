'use strict';

/**
 * induction-cycle controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::induction-cycle.induction-cycle');
